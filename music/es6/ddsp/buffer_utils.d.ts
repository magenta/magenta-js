export declare const arrayBufferToAudioBuffer: (audioCtx: AudioContext, arrayBuffer: Float32Array, sampleRate: number) => AudioBuffer;
export declare const sliceAudioBuffer: (audioCtx: AudioContext, audioBuffer: AudioBuffer, start: number, end: number, sampleRate: number) => AudioBuffer;
